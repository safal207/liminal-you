# LiminalDB Configuration
LIMINALDB_ENABLED=true
LIMINALDB_URL=ws://localhost:8001

# Storage Backend
# Options: memory | postgres | liminaldb
STORAGE_BACKEND=liminaldb

# PostgreSQL Configuration (if using postgres backend)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=liminal
POSTGRES_PASSWORD=youpass
POSTGRES_DB=liminal_you

# JWT Authentication
JWT_SECRET=liminal-you-secret-change-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24

# Feedback System
FEEDBACK_ENABLED=true

# CORS Configuration
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
